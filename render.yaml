services:
  - type: web
    name: dod-healthcare-api
    env: node
    rootDir: backend
    buildCommand: npm install --legacy-peer-deps
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
